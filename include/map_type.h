#ifndef DEF_MAP_TYPE_H
#define DEF_MAP_TYPE_H
#include <cstddef>

using column_type = std::size_t;
using row_type = std::size_t;
using biome_type = int;
using site_type = int;

#endif // !DEF_MAP_TYPE_H

